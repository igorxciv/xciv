import { StrictMode } from 'react'
import * as ReactDOM from 'react-dom'
import { Root } from '@website/app'

ReactDOM.render(
  <StrictMode>
    <Root />
  </StrictMode>,
  document.getElementById('root'),
)
